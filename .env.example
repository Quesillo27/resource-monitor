POSTGRES_DB=resource_monitor
POSTGRES_USER=monitor
POSTGRES_PASSWORD=change-this-db-password
JWT_SECRET=change-this-to-a-random-64-char-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-admin-password
SERVER_ADDR=:8080
RETENTION_DAYS=30
OFFLINE_AFTER_SECONDS=180
VITE_API_BASE_URL=http://localhost:8080
# Comma-separated allowed CORS origins. Leave empty to allow all (dev only).
ALLOWED_ORIGINS=http://localhost:3000

# Prefix SemVer del agente (sin sha). agent-assets compila <prefix>-<git-sha>
# y backend lo expone en /api/agent/version. Bumpear con scripts/set-version.sh.
AGENT_RELEASE_VERSION=v1.4.0
